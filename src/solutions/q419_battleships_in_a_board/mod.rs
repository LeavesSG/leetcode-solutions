/*
 * @lc app=leetcode id=419 lang=rust
 *
 * [419] Battleships in a Board
 */

// @lc code=start

use super::Solution;
#[allow(dead_code)]

impl Solution {
    pub fn count_battleships(board: Vec<Vec<char>>) -> i32 {
        fn bfs(
            board: &Vec<Vec<char>>,
            queue: Vec<(usize, usize)>,
            checked: std::collections::HashMap<(usize, usize), bool>,
        ) {
        }

        return 1;
    }
}
// @lc code=end
